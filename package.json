{
  "name": "expense-tracker-api",
  "version": "1.0.0",
  "description": "REST API for an Expense Tracker application built with Node.js, Express, MongoDB, Mongoose, and JWT authentication.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check app.js && node --check server.js && node --check config/env.js && node --check config/database.js && node --check routes/auth.js && node --check routes/expenses.js && node --check middleware/authMiddleware.js && node --check middleware/errorHandlers.js && node --check middleware/rateLimiters.js && node --check middleware/requestId.js && node --check models/User.js && node --check models/Expense.js",
    "test": "node --test --test-isolation=none",
    "validate": "npm run check && npm test"
  },
  "keywords": [
    "expense-tracker",
    "express",
    "mongodb",
    "mongoose",
    "jwt"
  ],
  "author": "Deb Gourab Biswas",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/debgourab/expense-tracker-api.git"
  },
  "bugs": {
    "url": "https://github.com/debgourab/expense-tracker-api/issues"
  },
  "homepage": "https://github.com/debgourab/expense-tracker-api#readme",
  "engines": {
    "node": ">=24 <25"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.2",
    "morgan": "^1.12.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "supertest": "^7.3.0",
    "yaml": "^2.9.1"
  }
}
